---
description: Look up FIASSE/SSEM reference material by topic, attribute, or section number
argument-hint: [topic, attribute, or section — e.g. "isolated integrity", "S4.4.1.2", "scoring"]
---

Run the **fiasse-lookup** skill for: $ARGUMENTS

Load and follow the skill at `${CLAUDE_PLUGIN_ROOT}/skills/fiasse-lookup/SKILL.md` (in a repo checkout: `skills/fiasse-lookup/SKILL.md`). Answer from the bundled `data/fiasse/` section files, citing section numbers — not from memory.
